# .github/workflows/update-problems.yml
name: Update Problems JSON
on:
  workflow_dispatch:  # Permite rodar manualmente

jobs:
  update_json:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Atualizar JSON
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}  # Token seguro!
        run: |
          # Aqui vai seu script para modificar problemas.json
          echo '{"titulo":"Teste","descricao":"Feito via Actions"}' > problemas.json
          
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add problemas.json
          git commit -m "Atualizado via Actions"
          git push
